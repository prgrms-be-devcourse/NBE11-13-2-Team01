spring:
  datasource:
    url: jdbc:mysql://localhost:3306/delivery_service
    username: root
    password: ${DB_PASSWORD}
  application:
    name: delivery_project
weather:
  api:
    key: ${WEATHER_API_KEY}